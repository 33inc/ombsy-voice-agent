services:
  - type: web
    name: ombsy-voice-agent
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: TELNYX_API_KEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false
